import { Injectable} from '@angular/core';
import { config } from 'app.config';


declare var require: any;
@Injectable({
        providedIn:'root' 
})

export class SnowflakeService
{
   
    constructor()
    {

    }

    getLoginInfo(userName: string, password_hash: string)
    {
        
    }
}